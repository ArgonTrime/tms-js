/*
Напишите функцию min(а,b), которая возвращает меньшее из чисел а,Б.
Пример вызовов:
min(2, 5)
min(3, -1)
min(1, 1)
*/

const min = (firstNumber, secondNumber ) => Math.min(firstNumber, secondNumber);

console.log(min(2, 5));
console.log(min(3, -1));
console.log(min(1, 1));